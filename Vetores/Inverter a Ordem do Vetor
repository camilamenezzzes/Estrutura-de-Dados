#Escreva um procedimento para ler um vetor X de 10 elementos inteiros. Logo após copie os
#elementos do vetor X para um vetor Y fazendo com que o 1o. elemento de X seja copiado para o 10.
#De Y, o 2o. de X para o 9o. de Y e assim sucessivamente. Após o término da cópia, imprimir o vetor Y.

tam = 10
vetor_X = [int] * tam
vetor_Y = [int] * tam

for i in range(len(vetor_X)):
  vetor_X[i] = int(input("Digite um número: \t"))

for j in range(len(vetor_Y)):
  vetor_Y[j] = vetor_X[i]
  i = i - 1

print("Y =", vetor_Y)
